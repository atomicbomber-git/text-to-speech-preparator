def normalize(chunked_text):
    chunked_text = chunked_text.replace('\n-\n', '-')
    chunked_text = chunked_text.replace('\', \'\"\')\n(KP', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(VP', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(NP', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G1', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G2', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G3', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G4', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G5', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G6', '')
    chunked_text = chunked_text.replace('\', \'\"\')\n(G7', '')
    chunked_text = chunked_text.replace('\n(KP', ',')
    chunked_text = chunked_text.replace('\n(VP', ',')
    chunked_text = chunked_text.replace('\n(NP', ',')
    chunked_text = chunked_text.replace('\n(G1', ',')
    chunked_text = chunked_text.replace('\n(G2', ',')
    chunked_text = chunked_text.replace('\n(G3', ',')
    chunked_text = chunked_text.replace('\n(G4', ',')
    chunked_text = chunked_text.replace('\n(G5', ',')
    chunked_text = chunked_text.replace('\n(G6', ',')
    chunked_text = chunked_text.replace('\n(G7', ',')
    chunked_text = chunked_text.replace('(KP', '')
    chunked_text = chunked_text.replace('(VP', '')
    chunked_text = chunked_text.replace('(NP', '')
    chunked_text = chunked_text.replace('(G1', '')
    chunked_text = chunked_text.replace('(G2', '')
    chunked_text = chunked_text.replace('(G3', '')
    chunked_text = chunked_text.replace('(G4', '')
    chunked_text = chunked_text.replace('(G5', '')
    chunked_text = chunked_text.replace('(G6', '')
    chunked_text = chunked_text.replace('(G7', '')
    chunked_text = chunked_text.replace('\", \'OP\')', '')
    chunked_text = chunked_text.replace('\", \'CP\')', '')
    chunked_text = chunked_text.replace('\", \'GM\')', '')
    chunked_text = chunked_text.replace('\", \';\')', '')
    chunked_text = chunked_text.replace('\", \':\')', '')
    chunked_text = chunked_text.replace('\", \'"\')\n(\'', '')
    chunked_text = chunked_text.replace('\", \'"\')', '')
    chunked_text = chunked_text.replace('\", \'-\')', '')
    chunked_text = chunked_text.replace('\", \'...\')', '')
    chunked_text = chunked_text.replace('\", \'JJ\')', '')
    chunked_text = chunked_text.replace('\", \'RB\')', '')
    chunked_text = chunked_text.replace('\", \'NNP\')', '')
    chunked_text = chunked_text.replace('\", \'NNG\')', '')
    chunked_text = chunked_text.replace('\", \'NN\')', '')
    chunked_text = chunked_text.replace('\", \'VBI\')', '')
    chunked_text = chunked_text.replace('\", \'VBT\')', '')
    chunked_text = chunked_text.replace('\", \'IN\')', '')
    chunked_text = chunked_text.replace('\", \'MD\')', '')
    chunked_text = chunked_text.replace('\", \'CC\')', '')
    chunked_text = chunked_text.replace('\", \'SC\')', '')
    chunked_text = chunked_text.replace('\", \'DT\')', '')
    chunked_text = chunked_text.replace('\", \'UH\')', '')
    chunked_text = chunked_text.replace('\", \'CDO\')', '')
    chunked_text = chunked_text.replace('\", \'CDC\')', '')
    chunked_text = chunked_text.replace('\", \'CDP\')', '')
    chunked_text = chunked_text.replace('\", \'CDI\')', '')
    chunked_text = chunked_text.replace('\", \'PRP\')', '')
    chunked_text = chunked_text.replace('\", \'WP\')', '')
    chunked_text = chunked_text.replace('\", \'PRN\')', '')
    chunked_text = chunked_text.replace('\", \'PRL\')', '')
    chunked_text = chunked_text.replace('\", \'NEG\')', '')
    chunked_text = chunked_text.replace('\", \'SYM\')', '')
    chunked_text = chunked_text.replace('\", \'RP\')', '')
    chunked_text = chunked_text.replace('\", \'FW\')', '')
    chunked_text = chunked_text.replace('\', \'OP\')', '')
    chunked_text = chunked_text.replace('\', \'CP\')', '')
    chunked_text = chunked_text.replace('\', \'GM\')', '')
    chunked_text = chunked_text.replace('\', \';\')', '')
    chunked_text = chunked_text.replace('\', \':\')', '')
    chunked_text = chunked_text.replace('\', \'"\')', '')
    chunked_text = chunked_text.replace('\', \'-\')', '')
    chunked_text = chunked_text.replace('\', \'...\')', '')
    chunked_text = chunked_text.replace('\', \'JJ\')', '')
    chunked_text = chunked_text.replace('\', \'RB\')', '')
    chunked_text = chunked_text.replace('\', \'NNP\')', '')
    chunked_text = chunked_text.replace('\', \'NNG\')', '')
    chunked_text = chunked_text.replace('\', \'NN\')', '')
    chunked_text = chunked_text.replace('\', \'VBI\')', '')
    chunked_text = chunked_text.replace('\', \'VBT\')', '')
    chunked_text = chunked_text.replace('\', \'IN\')', '')
    chunked_text = chunked_text.replace('\', \'MD\')', '')
    chunked_text = chunked_text.replace('\', \'CC\')', '')
    chunked_text = chunked_text.replace('\', \'SC\')', '')
    chunked_text = chunked_text.replace('\', \'DT\')', '')
    chunked_text = chunked_text.replace('\', \'UH\')', '')
    chunked_text = chunked_text.replace('\', \'CDO\')', '')
    chunked_text = chunked_text.replace('\', \'CDC\')', '')
    chunked_text = chunked_text.replace('\', \'CDP\')', '')
    chunked_text = chunked_text.replace('\', \'CDI\')', '')
    chunked_text = chunked_text.replace('\', \'PRP\')', '')
    chunked_text = chunked_text.replace('\', \'WP\')', '')
    chunked_text = chunked_text.replace('\', \'PRN\')', '')
    chunked_text = chunked_text.replace('\', \'PRL\')', '')
    chunked_text = chunked_text.replace('\', \'NEG\')', '')
    chunked_text = chunked_text.replace('\', \'SYM\')', '')
    chunked_text = chunked_text.replace('\', \'RP\')', '')
    chunked_text = chunked_text.replace('\', \'FW\')', '')
    chunked_text = chunked_text.replace('/OP))))', '')
    chunked_text = chunked_text.replace('/CP))))', '')
    chunked_text = chunked_text.replace('/GM))))', '')
    chunked_text = chunked_text.replace('/;))))', '')
    chunked_text = chunked_text.replace('/:))))', '')
    chunked_text = chunked_text.replace('/"))))', '')
    chunked_text = chunked_text.replace('/.))))', '')
    chunked_text = chunked_text.replace('/,))))', '')
    chunked_text = chunked_text.replace('/-))))', '')
    chunked_text = chunked_text.replace('/...))))', '')
    chunked_text = chunked_text.replace('/JJ))))', '')
    chunked_text = chunked_text.replace('/RB))))', '')
    chunked_text = chunked_text.replace('/NNP))))', '')
    chunked_text = chunked_text.replace('/NNG))))', '')
    chunked_text = chunked_text.replace('/NN))))', '')
    chunked_text = chunked_text.replace('/VBI))))', '')
    chunked_text = chunked_text.replace('/VBT))))', '')
    chunked_text = chunked_text.replace('/IN))))', '')
    chunked_text = chunked_text.replace('/MD))))', '')
    chunked_text = chunked_text.replace('/CC))))', '')
    chunked_text = chunked_text.replace('/SC))))', '')
    chunked_text = chunked_text.replace('/DT))))', '')
    chunked_text = chunked_text.replace('/UH))))', '')
    chunked_text = chunked_text.replace('/CDO))))', '')
    chunked_text = chunked_text.replace('/CDC))))', '')
    chunked_text = chunked_text.replace('/CDP))))', '')
    chunked_text = chunked_text.replace('/CDI))))', '')
    chunked_text = chunked_text.replace('/PRP))))', '')
    chunked_text = chunked_text.replace('/WP))))', '')
    chunked_text = chunked_text.replace('/PRN))))', '')
    chunked_text = chunked_text.replace('/PRL))))', '')
    chunked_text = chunked_text.replace('/NEG))))', '')
    chunked_text = chunked_text.replace('/SYM))))', '')
    chunked_text = chunked_text.replace('/RP))))', '')
    chunked_text = chunked_text.replace('/FW))))', '')
    chunked_text = chunked_text.replace('/OP)))', '')
    chunked_text = chunked_text.replace('/CP)))', '')
    chunked_text = chunked_text.replace('/GM)))', '')
    chunked_text = chunked_text.replace('/;)))', '')
    chunked_text = chunked_text.replace('/:)))', '')
    chunked_text = chunked_text.replace('/")))', '')
    chunked_text = chunked_text.replace('/.)))', '')
    chunked_text = chunked_text.replace('/,)))', '')
    chunked_text = chunked_text.replace('/-)))', '')
    chunked_text = chunked_text.replace('/...)))', '')
    chunked_text = chunked_text.replace('/JJ)))', '')
    chunked_text = chunked_text.replace('/RB)))', '')
    chunked_text = chunked_text.replace('/NNP)))', '')
    chunked_text = chunked_text.replace('/NNG)))', '')
    chunked_text = chunked_text.replace('/NN)))', '')
    chunked_text = chunked_text.replace('/VBI)))', '')
    chunked_text = chunked_text.replace('/VBT)))', '')
    chunked_text = chunked_text.replace('/IN)))', '')
    chunked_text = chunked_text.replace('/MD)))', '')
    chunked_text = chunked_text.replace('/CC)))', '')
    chunked_text = chunked_text.replace('/SC)))', '')
    chunked_text = chunked_text.replace('/DT)))', '')
    chunked_text = chunked_text.replace('/UH)))', '')
    chunked_text = chunked_text.replace('/CDO)))', '')
    chunked_text = chunked_text.replace('/CDC)))', '')
    chunked_text = chunked_text.replace('/CDP)))', '')
    chunked_text = chunked_text.replace('/CDI)))', '')
    chunked_text = chunked_text.replace('/PRP)))', '')
    chunked_text = chunked_text.replace('/WP)))', '')
    chunked_text = chunked_text.replace('/PRN)))', '')
    chunked_text = chunked_text.replace('/PRL)))', '')
    chunked_text = chunked_text.replace('/NEG)))', '')
    chunked_text = chunked_text.replace('/SYM)))', '')
    chunked_text = chunked_text.replace('/RP)))', '')
    chunked_text = chunked_text.replace('/FW)))', '')
    chunked_text = chunked_text.replace('/OP))', '')
    chunked_text = chunked_text.replace('/CP))', '')
    chunked_text = chunked_text.replace('/GM))', '')
    chunked_text = chunked_text.replace('/;))', '')
    chunked_text = chunked_text.replace('/:))', '')
    chunked_text = chunked_text.replace('/"))', '')
    chunked_text = chunked_text.replace('/.))', '')
    chunked_text = chunked_text.replace('/,))', '')
    chunked_text = chunked_text.replace('/-))', '')
    chunked_text = chunked_text.replace('/...))', '')
    chunked_text = chunked_text.replace('/JJ))', '')
    chunked_text = chunked_text.replace('/RB))', '')
    chunked_text = chunked_text.replace('/NNP))', '')
    chunked_text = chunked_text.replace('/NN))', '')
    chunked_text = chunked_text.replace('/NNG))', '')
    chunked_text = chunked_text.replace('/VBI))', '')
    chunked_text = chunked_text.replace('/VBT))', '')
    chunked_text = chunked_text.replace('/IN))', '')
    chunked_text = chunked_text.replace('/MD))', '')
    chunked_text = chunked_text.replace('/CC))', '')
    chunked_text = chunked_text.replace('/SC))', '')
    chunked_text = chunked_text.replace('/DT))', '')
    chunked_text = chunked_text.replace('/UH))', '')
    chunked_text = chunked_text.replace('/CDO))', '')
    chunked_text = chunked_text.replace('/CDC))', '')
    chunked_text = chunked_text.replace('/CDP))', '')
    chunked_text = chunked_text.replace('/CDI))', '')
    chunked_text = chunked_text.replace('/PRP))', '')
    chunked_text = chunked_text.replace('/WP))', '')
    chunked_text = chunked_text.replace('/PRN))', '')
    chunked_text = chunked_text.replace('/PRL))', '')
    chunked_text = chunked_text.replace('/NEG))', '')
    chunked_text = chunked_text.replace('/SYM))', '')
    chunked_text = chunked_text.replace('/RP))', '')
    chunked_text = chunked_text.replace('/FW))', '')
    chunked_text = chunked_text.replace('/OP)', '')
    chunked_text = chunked_text.replace('/CP)', '')
    chunked_text = chunked_text.replace('/GM)', '')
    chunked_text = chunked_text.replace('/;)', '')
    chunked_text = chunked_text.replace('/:)', '')
    chunked_text = chunked_text.replace('/")', '')
    chunked_text = chunked_text.replace('/.)', '')
    chunked_text = chunked_text.replace('/,)', '')
    chunked_text = chunked_text.replace('/-)', '')
    chunked_text = chunked_text.replace('/...)', '')
    chunked_text = chunked_text.replace('/JJ)', '')
    chunked_text = chunked_text.replace('/RB)', '')
    chunked_text = chunked_text.replace('/NNP)', '')
    chunked_text = chunked_text.replace('/NN)', '')
    chunked_text = chunked_text.replace('/NNG)', '')
    chunked_text = chunked_text.replace('/VBI)', '')
    chunked_text = chunked_text.replace('/VBT)', '')
    chunked_text = chunked_text.replace('/IN)', '')
    chunked_text = chunked_text.replace('/MD)', '')
    chunked_text = chunked_text.replace('/CC)', '')
    chunked_text = chunked_text.replace('/SC)', '')
    chunked_text = chunked_text.replace('/DT)', '')
    chunked_text = chunked_text.replace('/UH)', '')
    chunked_text = chunked_text.replace('/CDO)', '')
    chunked_text = chunked_text.replace('/CDC)', '')
    chunked_text = chunked_text.replace('/CDP)', '')
    chunked_text = chunked_text.replace('/CDI)', '')
    chunked_text = chunked_text.replace('/PRP)', '')
    chunked_text = chunked_text.replace('/WP)', '')
    chunked_text = chunked_text.replace('/PRN)', '')
    chunked_text = chunked_text.replace('/PRL)', '')
    chunked_text = chunked_text.replace('/NEG)', '')
    chunked_text = chunked_text.replace('/SYM)', '')
    chunked_text = chunked_text.replace('/RP)', '')
    chunked_text = chunked_text.replace('/FW)', '')
    chunked_text = chunked_text.replace('/OP', '')
    chunked_text = chunked_text.replace('/CP', '')
    chunked_text = chunked_text.replace('/GM', '')
    chunked_text = chunked_text.replace('/;', '')
    chunked_text = chunked_text.replace('/:', '')
    chunked_text = chunked_text.replace('/"', '')
    chunked_text = chunked_text.replace('/.', '')
    chunked_text = chunked_text.replace('/,', '')
    chunked_text = chunked_text.replace('/-', '')
    chunked_text = chunked_text.replace('/...', '')
    chunked_text = chunked_text.replace('/JJ', '')
    chunked_text = chunked_text.replace('/RB', '')
    chunked_text = chunked_text.replace('/NNP', '')
    chunked_text = chunked_text.replace('/NNG', '')
    chunked_text = chunked_text.replace('/NN', '')
    chunked_text = chunked_text.replace('/VBI', '')
    chunked_text = chunked_text.replace('/VBT', '')
    chunked_text = chunked_text.replace('/IN', '')
    chunked_text = chunked_text.replace('/MD', '')
    chunked_text = chunked_text.replace('/CC', '')
    chunked_text = chunked_text.replace('/SC', '')
    chunked_text = chunked_text.replace('/DT', '')
    chunked_text = chunked_text.replace('/UH', '')
    chunked_text = chunked_text.replace('/CDO', '')
    chunked_text = chunked_text.replace('/CDC', '')
    chunked_text = chunked_text.replace('/CDP', '')
    chunked_text = chunked_text.replace('/CDI', '')
    chunked_text = chunked_text.replace('/PRP', '')
    chunked_text = chunked_text.replace('/WP', '')
    chunked_text = chunked_text.replace('/PRN', '')
    chunked_text = chunked_text.replace('/PRL', '')
    chunked_text = chunked_text.replace('/NEG', '')
    chunked_text = chunked_text.replace('/SYM', '')
    chunked_text = chunked_text.replace('/RP', '')
    chunked_text = chunked_text.replace('/FW', '')
    chunked_text = chunked_text.replace(' .', '.')
    chunked_text = chunked_text.replace(' :', ':')
    chunked_text = chunked_text.replace(':,', ':')
    chunked_text = chunked_text.replace(' ,', ',')
    chunked_text = chunked_text.replace('.\n', '.')
    chunked_text = chunked_text.replace('\n.', '.')
    chunked_text = chunked_text.replace('\n,', ',')
    chunked_text = chunked_text.replace('\n', '')
    chunked_text = chunked_text.replace(',,,', ',')
    chunked_text = chunked_text.replace(',,', ',')
    chunked_text = chunked_text.replace(',.', '.')
    chunked_text = chunked_text.replace('.,', '.')
    chunked_text = chunked_text.replace(',)', ')')
    chunked_text = chunked_text.replace('(,', '(')
    chunked_text = chunked_text.replace('(\"', ' ')
    chunked_text = chunked_text.replace('(\'', ' ')
    chunked_text = chunked_text.replace('( ', '(')
    chunked_text = chunked_text.replace(' )', ')')
    chunked_text = chunked_text.replace('    ', ' ')
    chunked_text = chunked_text.replace('   ', ' ')
    chunked_text = chunked_text.replace('  ', ' ')
    return chunked_text
